package com.controlcalidad.service;

/**
 * Interfaz específica para Rol.
 * Extiende IGenericService<Rol, Integer> para heredar
 * los 5 métodos CRUD sin necesidad de redefinirlos.
 */
public interface IRolService extends IGenericService<Rol, Integer> {
    // Aquí se pueden agregar métodos específicos de Rol si se necesitan
}
